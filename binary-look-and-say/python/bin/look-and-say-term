#! /usr/bin/env python

import sys

from conway.say import Sayer

if (len(sys.argv) == 2):
   result, sayer, index = '1', Sayer(), int(sys.argv[1])
   while (index > 1):
      result = sayer.say(result)
      index -= 1
   print result;
else:
  print "usage: {0} <n>".format(sys.argv[0])
